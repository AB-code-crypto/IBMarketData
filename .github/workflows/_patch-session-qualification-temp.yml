name: Temporary session qualification patch

on:
  push:
    branches:
      - agent/session-calendar-qualification

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/session-calendar-qualification
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/_patch_session_qualification_temp.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            src/ibmd/catalog/sessions.py \
            src/ibmd/catalog/resolver.py \
            src/ibmd/execution/application/liquidation_triggers.py \
            tester/target_catalog_tester.py
          git diff --cached --quiet || git commit -m "Add bounded session qualification coverage"
          git push origin HEAD:agent/session-calendar-qualification
